**To install a new package from terminal (admin), for example $\texttt{dplyr}$**

`Rscript -e "install.packages('dplyr', repos='http://cran.rstudio.com/')"`


**To render a .Rmd**

`Rscript -e "rmarkdown::render('src/script.Rmd')"`